all:
  children:
    windows:
      hosts:
        win-serv-22:                        # 👈 Logical name (alias)
          ansible_host: 10.32.20.254        # 👈 IP of the Windows host
          ansible_user: Administrator       # 👈 Windows user
          ansible_password: "P@ssword1234"  # 👈 Secure password (make sure to protect this file)
          ansible_connection: winrm         # 👈 Required for Windows
          ansible_winrm_transport: basic    # 👈 Authentication method
          ansible_port: 5985                # 👈 Default for HTTP-based WinRM
